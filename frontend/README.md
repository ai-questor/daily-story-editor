# frontend

## 환경설정
```
cd frontend
npm create vite@latest .
  Ignore files and continue -> daily-story-editor -> React -> Typescript -> No -> Yes
npm install
```

## 실행하기
```
npm run dev -- --host 0.0.0.0
```